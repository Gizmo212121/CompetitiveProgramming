#include <bits/stdc++.h>

int main()
{
    std::cin.tie(NULL);
    std::ios::sync_with_stdio(false);

    int testCases;
    std::cin >> testCases;

    for (int i = 0; i < testCases; i++)
    {
        
    }
}
